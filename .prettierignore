# Jekyll build output + caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler (Ruby gems)
vendor/
.bundle/

# Node
node_modules/
package-lock.json

# Generated by release-please on every release — its output style is not Prettier's,
# so formatting it just fights the generator (matches hyperweaver-server).
CHANGELOG.md

# Generated by scripts/generate-docs.js at build (regenerated every build) — never
# hand-formatted: the changelogs carry raw external markdown wrapped in {% raw %}.
changelogs/
